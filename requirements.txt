# Runtime dependencies. Pinned to versions verified against the test suite.
# Refresh by running tests on a fresh `uv pip install -r requirements.txt` and
# updating these pins.
altair==6.1.0
google-auth==2.50.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.3.1
gspread==6.2.1
matplotlib==3.10.9
numpy==2.4.4
pandas==3.0.2
python-dateutil==2.9.0.post0
seaborn==0.13.2
streamlit==1.58.0
